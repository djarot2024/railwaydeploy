{
  "build": {
    "src": "public",
    "use": "php",
    "cmd": "composer install && php artisan migrate"
  },
  "deploy": {
    "src": "public",
    "use": "php",
    "cmd": "php artisan serve --host 0.0.0.0 --port $PORT"
  }
}
